version: '3.8'

services:
  portfolio:
    build: .
    container_name: curriculo-dev
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true